# MCP Tutorial Series

## Install

* install `uv` ([Intalling uv](https://docs.astral.sh/uv/getting-started/installation/))
* generate your virtual env: `uv venv`, then activate it

## Run

```bash
python main.py
```

or `uv run main.py` if your virtual env is not activated ;)
